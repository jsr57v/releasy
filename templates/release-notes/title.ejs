<% if(true || features.length > 0 || fixes.length > 0 ) { // FIXME vigilar si el true aquí funciona bien y se puede quitar el if%>
:rocket: *New <%= appName %> Release: <%- release.link %> - [<%= release.date %> - <%= release.time %>]*  :rocket:
<% } %>